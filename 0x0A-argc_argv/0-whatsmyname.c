#include <stdio.h>

/**
* main - prints its name
* @argc: argument count
* @argv: pointers to array of string
* Return: Always (0)
*/
int main(int argc, char *argv[])
{
printf("%s\m", *argv);
return (0);
}
