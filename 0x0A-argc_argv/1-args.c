#include <stdio.h>

/**
* main - prints the number of
* argumments passed into it
* @argc: argument
* @argv: array of strings
* Return: Always (0)
*/
int main(int argc, char *argv[])
{
argv = argv;
printf("%d\n", argc - 1);
return (0);
}
